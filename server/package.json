{
    "name": "st-multiplayer",
    "version": "1.0.0",
    "private": true,
    "description": "Local relay for the SillyTavern Multiplayer extension.",
    "type": "module",
    "main": "index.js",
    "engines": {
        "node": ">=18"
    },
    "peerDependencies": {
        "ws": "^8.18.0"
    },
    "comment": "ws ships with SillyTavern 1.13+ and resolves from the parent node_modules. If your install predates that, run `npm install ws` in the SillyTavern root."
}
